# Automatic-Movie-Picker
This python script works offline
Place this script inside the folder where you store all your movies

It can also play selected movie automatically, if the folder consist of more than one video file it will only play first video

Each movie must be stored in seperate folder inside main movie collection folder.
